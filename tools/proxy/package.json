{
  "name": "mihaku-proxy",
  "version": "1.1.0",
  "private": true,
  "description": "Local proxy server — routes mihaku AI requests through Claude CLI (Max subscription, $0)",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  }
}
